Attribute VB_Name = "Module1"
'Make the variable NumMatch a module, so that it can be used to search if the match from the
'trivia are greater than 70% on one page, use this variable on another page to process the
'discount and post the savings.
Public NumMatches As Integer
